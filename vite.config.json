import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // Critical for GitHub Pages: ensures assets use relative paths
  // instead of absolute paths (e.g. "./script.js" vs "/script.js")
  base: './', 
})
